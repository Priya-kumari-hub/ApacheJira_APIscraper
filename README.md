# Apache Jira Scraper & JSONL Dataset Builder

This project is a web scraping + data transformation pipeline that collects public issue data from Apache Jira (for 3 projects: SPARK, HADOOP, KAFKA) and converts it into a clean, structured JSONL dataset suitable for LLM training.
It includes:
Reliable Jira scraping
Pagination handlin
Retry + timeout logic
Error handling for 429, 5xx
JSON → JSONL transformation
Task generation (summarization + classification)
Large dataset links (stored on Google Drive)
This repository contains the full code + documentation to reproduce the dataset.

## Repository Structure
Apache-jira-scraper
│
├── scrape_jira.py        # Scrapes SPARK, HADOOP, KAFKA issues
├── transform.py          # Converts raw → JSONL (LLM-ready)
├── requirements.txt      # Dependencies
└── README.md             # Documentation

## Dataset Downloads (Google Drive)
🔹 Transformed JSONL (LLM training ready)

These are cleaned + processed datasets.

Project	File	Drive Link
SPARK	spark_jira.jsonl	(https://drive.google.com/file/d/1IEFlQrBLLRMmMQWlqvm_s3F2jTS2rzjf/view?usp=drive_link)
HADOOP	hadoop_jira.jsonl	(https://drive.google.com/file/d/1kNjnSHuE91QsMyQJAQ5dJB4TKL08TUBb/view?usp=drive_link)
KAFKA	kafka_jira.jsonl	(https://drive.google.com/file/d/1BtGyvemnlHJ2Vi9ZGVu4_E6wgld28GH8/view?usp=drive_link)

## Project Overview

This scraper uses the Apache Jira REST API to fetch issues and their:
Title, description, reporter
Status, priority, metadata
Comments
Timestamps
Labels
It then transforms the data into a JSONL format commonly used for fine-tuning LLMs, and adds optional autogenerated tasks like:Summarization tasks
Classification task
This makes the dataset directly usable for training chatbots, support bots, or issue-resolution models.

### Features
#### Pagination Support
Fetches all issues even when Jira splits them across pages.
#### Retry & Timeout Logic
Handles network failures gracefully:
Retries 3 times
Waits if rate limited (HTTP 429)
Skips 5xx server errors safely
#### JSONL Transformation
Converts raw issue fields into meaningful structured objects.
#### LLM Task Generation
"task_summarization": "Summarize the issue: <title>",
"task_classification": "Classify issue priority: <title>"




